---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contacto - Enia">
  <main>
    <section class="contact-card">
      <h1>Contactar a <span class="highlight">Enia</span></h1>
      <p>Asegure su suministro de estructuras de red analógica.</p>
      
      <form action="/success" method="GET">
        <div class="form-group">
          <label for="name">Identificador (Nombre)</label>
          <input type="text" id="name" name="name" required placeholder="ej. Acme Corp Inc." />
        </div>
        
        <div class="form-group">
          <label for="size">Masa Organizacional</label>
          <select id="size" name="size">
            <option>Etapa Semilla (1-10)</option>
            <option>Serie A (10-50)</option>
            <option>Empresa (50+)</option>
            <option>Imperio Galáctico</option>
          </select>
        </div>

        <div class="form-group">
          <label for="message">Carga de Datos</label>
          <textarea id="message" name="message" rows="4" placeholder="Especificar parámetros de textura..."></textarea>
        </div>

        <button type="submit" class="btn">Transmitir Solicitud</button>
      </form>
    </section>
  </main>
</Layout>

<style>
  main {
    min-height: 80vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 8rem 2rem 2rem;
  }

  .contact-card {
    background: rgba(5, 5, 5, 0.8);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(0, 243, 255, 0.2);
    padding: 3rem;
    border-radius: 16px;
    width: 100%;
    max-width: 500px;
    box-shadow: 0 0 50px rgba(0, 243, 255, 0.1);
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
    text-align: center;
  }

  .highlight {
    color: var(--color-primary);
  }

  p {
    text-align: center;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 2rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  label {
    display: block;
    font-family: var(--font-heading);
    font-size: 0.8rem;
    margin-bottom: 0.5rem;
    color: var(--color-secondary);
  }

  input, select, textarea {
    width: 100%;
    padding: 0.8rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 4px;
    color: white;
    font-family: var(--font-body);
    transition: all 0.3s ease;
  }

  input:focus, select:focus, textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 10px rgba(0, 243, 255, 0.2);
  }

  .btn {
    width: 100%;
    padding: 1rem;
    background: var(--color-primary);
    color: var(--color-bg);
    border: none;
    border-radius: 4px;
    font-family: var(--font-heading);
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 1rem;
  }

  .btn:hover {
    background: white;
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
  }
</style>
